# Docker Django

### Build and run
#### Build with python3
* `docker build -t reliable .`
* `docker run -d -p 80:80 reliable`
* go to 127.0.0.1 to see if works
### Build and run
### Build and run
### Build and run